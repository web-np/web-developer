// JavaScript source code
// progress bar auto count with %
//countTab(r,i) { r = (i + 1) / r * 100;--r= elements; i=posizion of itm in loop
//location.href.split('/').reverse()[0];
//insertAdjacentHTML("beforeend","data");----append data
